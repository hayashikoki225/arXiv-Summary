The summary for today already exists at `hep-ex/2026-07-05.md`, and I've verified it against the live `hep-ex/list/new` — **it's accurate and current**. No regeneration is needed. Here's why:

Today (07-05, Sunday) is a weekend, so arXiv's `hep-ex/new` is still showing the **July 3 batch** (3 new, 7 cross-lists, 8 replacements — exactly the 18 entries in the existing file). The next fresh batch won't post until the 07-06 distribution. The existing file already:

- Correctly notes the weekend hold-over at the top
- Reprioritizes the batch around your interests (cavity/quantum-sensor DM & HFGW, SUSY/DM, novel techniques) rather than just mirroring `example.md`

**The picks aligned with your interests (already in the file):**
- **[16] Cavity Multimodes as an Array for HFGW** (`2601.03341`) — single microwave cavity's multiple EM modes act as a pseudo-array to reconstruct HFGW direction/polarization. Directly on your cavity×sensing axis.
- **[11] RF-excited magnetic field axion haloscope** (`2408.16229`) — transverse RF modulation to access the *first-order* axion-photon response, claimed 0.3–1 order sensitivity gain.
- **[14] Higgsino luminous DM** (`2409.07768`) — SUSY×DM direct hit via luminous up-scatter in JUNO/SNO+/KamLAND.
- **[3] PandaX-4T solar pp-ν** (`2607.02405`) — LXe DM detector as ν observatory.
- Novel-technique: [2] point-cloud heavy-flavor electron ID, [5] triggerless 4D detectors, [7] IceCube single-PMT ns timing, [12] MEG II Transformer tracking.

**quant-ph cross-lists into hep-ex: zero today.**

Two things worth flagging:
1. **`hep-ex/2026-07-04.md` is empty (0 bytes)** — looks like a failed run. Want me to fill it in? (Note: 07-04 was also a weekend, so it'd be the same 07-03 batch.)
2. I did **not** overwrite the existing 07-05 file since its content already matches the live listing exactly. If you'd prefer I regenerate it fresh anyway, or pull the actual `quant-ph/new` listing and summarize that (background-heavy, per your prefs), just say so.
